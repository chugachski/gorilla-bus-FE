import React from 'react';
import Matrix from './utils/matrix';
import eta from './utils/eta.js';

function Derp(props){
  //if toSeward is true,
  eta(props)
  // console.log(derp);
  return (
      <div></div>
  );
}

export default Derp
