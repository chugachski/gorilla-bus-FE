import React from 'react';
import Matrix from './utils/matrix';

function Derp(props){
    let derp = Matrix.getTime(props)
    console.log(derp);
    return (
        <div></div>
      );
  }

export default Derp
